name: Auto Comment on Issue

on:
  issues:
    types: [opened]

permissions:
  issues: write

jobs:
  comment:
    runs-on: ubuntu-latest

    steps:
      - name: Add Comment to Issue
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.payload.issue.number,
              body: `Thank you for raising this issue! 🙌

Please make sure that:
- This issue is **not a duplicate** 👉 https://github.com/Shubham-cyber-prog/100-Days-Of-Web-Development-ECWoC26/issues
- You have carefully read our **CONTRIBUTING.md** 👉 https://github.com/Shubham-cyber-prog/100-Days-Of-Web-Development-ECWoC26/blob/main/CONTRIBUTING.md

We appreciate your contribution to **100 Days Of Web Development – ECWoC'26** 🚀✨`
            });
