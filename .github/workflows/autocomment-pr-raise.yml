name: Auto Comment on PR

on:
  pull_request_target:
    types: [opened]

permissions:
  pull-requests: write
  issues: write

jobs:
  comment:
    runs-on: ubuntu-latest

    steps:
      - name: Add Comment to Pull Request
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.payload.pull_request.number,
              body: `Thank you for submitting this Pull Request! 🙌✨

Please ensure that:
- Your changes follow our **CONTRIBUTING.md** 👉 https://github.com/Shubham-cyber-prog/100-Days-Of-Web-Development-ECWoC26/blob/main/CONTRIBUTING.md
- The PR title and description clearly explain the changes
- Related issues (if any) are properly linked

Our maintainers will review your PR as soon as possible 🚀  
Thank you for contributing to **100 Days Of Web Development – ECWoC'26** 💙`
            });
